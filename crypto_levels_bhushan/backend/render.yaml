services:
  - type: web
    name: crypto-levels-backend
    env: python
    runtime: python-3.12.8
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: TWELVE_DATA_API_KEY
        sync: false
      - key: DB_NAME
        value: delta_tracker
      - key: COLLECTION_NAME
        value: monitored_scrips
