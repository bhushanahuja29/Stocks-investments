import React from 'react';
import InstallPrompt from './InstallPrompt';

/** Global PWA install + notification shell (mirrors Gymtra PwaShell). */
function PwaShell() {
  return <InstallPrompt />;
}

export default PwaShell;
